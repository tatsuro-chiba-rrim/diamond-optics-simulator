window.__htmlChunks=window.__htmlChunks||[];window.__htmlChunks.push("race.exitDir;\n  spreadAngle.textContent = (blue && red) ? (V.ang(blue,red)*180/Math.PI).toFixed(2)+\"°\" : \"—\";\n\n  const yellowish = result.rays.find(r => r.w === 590);\n  exitFace.textContent = yellowish && yellowish.trace.exitFace ? yellowish.trace.exitFace : \"—\";\n\n  const blend = computeBlendedExitColor(result);\n  if(blend){\n    const R8 = Math.round(blend.r*255);\n    const G8 = Math.round(blend.g*255);\n    const B8 = Math.round(blend.b*255);\n    blendSwatch.style.background = `rgb(${R8},${G8},${B8})`;\n    blendColorText.textContent = `rgb(${R8},${G8},${B8})`;\n  }else{\n    blendSwatch.style.background = \"transparent\";\n    blendColorText.textContent = \"—\";\n  }\n\n  if(exited.length===0) pathState.textContent = \"no exit\";\n  else if(exited.length < result.rays.length) pathState.textContent = \"partial dispersion\";\n  else pathState.textContent = \"full spectral exit\";\n}\n\n// ---------- loop ----------\nlet running=true, t=0, lastResult=null, last=performance.now();\n\nfunction simulationStep(dt){\n  const nD = parseFloat(nSlider.value);\n  t += dt;\n  const result = traceWhite(nD, t);\n  lastResult = result;\n  result.rays.forEach(r => {\n    if(r.trace.exitDir) addExitDirection(r.trace.exitDir, r, Math.max(0.02, spectralWeight(r)));\n  });\n  updateStats(result, nD);\n  drawProjectionPanel(result);\n  draw3D(result);\n  drawScatter();\n  updateWhiteout(result);\n  updateWhiteout(result);\n}\nfunction redrawOnly(){\n  const nD = parseFloat(nSlider.value);\n  const result = traceWhite(nD, t);\n  lastResult = result;\n  updateStats(result, nD);\n  drawProjectionPanel(result);\n  draw3D(result);\n  drawScatter();\n}\nfunction loop(now){\n  const dt=Math.min(.05,(now-last)/1000);\n  last=now;\n  if(running) simulationStep(dt * animationSpeed);\n  else if(lastResult){\n    drawProjectionPanel(lastResult);\n    draw3D(lastResult);\n    drawScatter();\n    updateWhiteout(lastResult);\n  }\n  requestAnimationFrame(loop);\n}\n\n// ---------- UI ----------\nmaterialPreset.addEventListener(\"change\",()=>{\n  applyMaterialPreset(materialPreset.value);\n});\n\nnSlider.addEventListener(\"input\",()=>{\n  if(materialPreset.value !== \"custom\"){\n    materialPreset.value = \"custom\";\n    activeMaterialKey = \"custom\";\n    MATERIALS.custom.nD = parseFloat(nSlider.value);\n    // Keep the dispersion of the last selected material for manual nD tuning.\n    MATERIALS.custom.dispersion = currentDispersion;\n    MATERIALS.custom.whitePriority = MATERIALS[activeMaterialKey]?.whitePriority || MATERIALS.custom.whitePriority;\n  }\n  materialInfo.textContent = `Custom: nD ${parseFloat(nSlider.value).toFixed(3)} / Δn ${currentDispersion.toFixed(4)} / W ${currentDispersion.toFixed(2)}`;\n  materialName.textContent = \"Custom\";\n  clearBins();\n  redrawOnly();\n});\nvisualMode.addEventListener(\"change\",()=>{\n  updateWhiteout(lastResult);\n});\n\nspeedSlider.addEventListener(\"input\",()=>{\n  animationSpeed = parseFloat(speedSlider.value);\n  speedValue.textContent = `${animationSpeed.toFixed(2)}×`;\n});\n\ntoggleBtn.addEventListener(\"click\",()=>{\n  running=!running;\n  toggleBtn.textContent=running?\"Pause\":\"Start\";\n  statusEl.textContent=running?\"running\":\"paused\";\n});\nstepBtn.addEventListener(\"click\",()=>{\n  if(running){\n    running=false;\n    toggleBtn.textContent=\"Start\";\n    statusEl.textContent=\"paused\";\n  }\n  simulationStep(.14);\n});\nclearBtn.addEventListener(\"click\",()=>{\n  clearBins();\n  drawScatter();\n});\n\nview3d.addEventListener(\"pointerdown\",e=>{\n  dragging=true; px=e.clientX; py=e.clientY;\n  view3d.setPointerCapture(e.pointerId);\n});\nview3d.addEventListener(\"pointermove\",e=>{\n  if(!dragging) return;\n  yaw+=(e.clientX-px)*.008;\n  pitch+=(e.clientY-py)*.006;\n  pitch=Math.max(-1.1,Math.min(1.1,pitch));\n  px=e.clientX; py=e.clientY;\n  if(lastResult){\n    draw3D(lastResult);\n    updateWhiteout(lastResult);\n  }\n});\nview3d.addEventListener(\"pointerup\",()=>dragging=false);\nview3d.addEventListener(\"pointercancel\",()=>dragging=false);\nwindow.addEventListener(\"resize\",()=>{ redrawOnly(); updateWhiteout(lastResult); });\n\nclearBins();\nspeedValue.textContent = `${animationSpeed.toFixed(2)}×`;\nflashValue.textContent = \"flash 0.00\";\napplyMaterialPreset(\"diamond\");\nrequestAnimationFrame(loop);\n\n})();\n</script>\n<footer class=\"public-footer\">\n  <div class=\"footgrid\">\n    <div>\n      <b>Round Brilliant White Light Dispersion Simulator</b><br>\n      白色光の分散、材質差、戻り光の見え方を可視化する公開用試作ページです。<br>\n      <span class=\"small\">作成：千葉達朗（アジア航測 先端技術研究所）</span>\n    </div>\n    <div>\n      <b>免責・注意</b><br>\n      本ページは教育・研究・可視化のための簡略シミュレーションです。厳密な光学設計、宝石鑑別、製品評価のための計算結果を保証するものではありません。<br>\n      <span class=\"small\">複屈折など一部の性質は簡略化しています。</span>\n    </div>\n    <div>\n      <b>動作</b><br>\n      ブラウザ上で動作する 1 ファイル HTML です。追加のサーバー計算は不要です。<br>\n      <span class=\"small\">材質プリセット：Diamond / Quartz / Glass / Sapphire / CZ / Moissanite ほか</span>\n    </div>\n    <div>\n      <b>公開情報・連絡先</b><br>\n      公開・共有は自由に行えますが、出典表示として作成者名を残すことを推奨します。<br>\n      <span class=\"small\">\n        Facebook:\n        <a href=\"https://www.facebook.com/arukazan\" target=\"_blank\" rel=\"noopener noreferrer\">\n          https://www.facebook.com/arukazan\n        </a><br>\n        お問い合わせ：上記 Facebook ページ／メッセンジャー\n      </span><br>\n      <span class=\"small\">Version: public footer edition</span>\n    </div>\n  </div>\n</footer>\n</body>\n</html>\n");
